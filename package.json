{
  "name": "team-taboo-araby",
  "version": "1.0.0",
  "private": true,
  "description": "Team Taboo — realtime bilingual (franco + Arabic) party game on Cloudflare Workers + Durable Objects",
  "scripts": {
    "build": "npm --prefix client install && npm --prefix client run build",
    "dev": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy",
    "test": "node scripts/testMatching.js"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
