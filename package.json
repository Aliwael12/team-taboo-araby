{
  "name": "team-taboo",
  "version": "1.0.0",
  "private": true,
  "description": "Team Taboo — realtime multiplayer word-describing party game (Node + Socket.io + React)",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server/index.js",
    "build": "npm --prefix client install && npm --prefix client run build",
    "dev:server": "node --watch server/index.js",
    "dev:client": "npm --prefix client run dev",
    "dev": "concurrently -n server,client -c blue,green \"npm run dev:server\" \"npm run dev:client\"",
    "test": "node scripts/testMatching.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "socket.io": "^4.7.5"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
